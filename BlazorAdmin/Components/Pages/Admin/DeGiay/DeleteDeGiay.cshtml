@* @using API.Models.DTO
@inherits LayoutComponentBase

<div class="popup-overlay">
    <div class="popup-form card p-4 shadow text-center">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h5 class="mb-0">❌ Xác nhận xoá</h5>
            <button class="btn-close" @onclick="() => OnCancel.InvokeAsync()" aria-label="Close"></button>
        </div>

        <p>Bạn có chắc muốn xoá <strong>@Item?.TenDeGiay</strong> không?</p>

        <div class="d-flex justify-content-center mt-3">
            <button class="btn btn-danger me-2" @onclick="() => OnConfirm.InvokeAsync(Item)">Xoá</button>
            <button class="btn btn-secondary" @onclick="() => OnCancel.InvokeAsync()">Huỷ</button>
        </div>
    </div>
</div>

@code {
    [Parameter] public DeGiayDTO? Item { get; set; }
    [Parameter] public EventCallback<DeGiayDTO?>
    OnConfirm { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }
    }
 *@